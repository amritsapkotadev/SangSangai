{
  "name": "sangpoints-blockchain",
  "version": "1.0.0",
  "description": "SangSangai blockchain layer — SangPoints smart contract on Polygon Amoy",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:amoy": "hardhat run scripts/deploy.js --network amoy",
    "seed:demo": "hardhat run scripts/seedDemo.js --network amoy",
    "verify": "hardhat verify --network amoy"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "ethers": "^6.13.0",
    "dotenv": "^16.4.5"
  }
}